<div class="dashboard">
    <div class="title-container">
        <h2>Dashboard</h2>
        <a class="sys-btn" href="/thoughts/add">Create Thought</a>
    </div>
    <div class="grid-container">
        {{#each thoughts}}
        <div class="card" id="{{this.id}}">
            <h3>
                <span class="card-actions">
                    <i class="bi bi-pencil-fill" title="Edit" data-id="Edit"></i>
                    <i class="bi bi-x-lg" title="Remove" data-id="Remove"></i>
                </span>
                {{this.title}}
            </h3>
        </div>
        {{/each}}
    </div>
</div>